---
title: Home
---
<p>
  This site collects some common approaches to building Lume sites in use across Open Innovations.
</p>

{{ for technique of techniques.map(x => search.page(`technique title=${x}`)).filter(x => x) }}
  <h2>{{ technique.title }}</h2>
  {{ comp.link.Grid({ links: technique.catalogue.toSorted((a, b) => a.title > b.title ? 1 : -1) }) }}
{{ /for }}

